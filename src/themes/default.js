import type { Theme } from '../types';

const theme: Theme = {
  listStyle: {
    listStyle: 'none',
  },
  nodeStyle: {
    padding: 5,
    backgroundColor: '#f3f3f3',
    marginTop: 2,
    marginBottom: 2,
  },
};

export default theme;
